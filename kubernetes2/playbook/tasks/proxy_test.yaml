
### THIS FILE IS GENERATED BY setup.rb ###
- name: start pod directly
  shell: |
    cat <<EOF >/etc/kubelet.d/test-pod.yaml
    apiVersion: v1
    kind: Pod
    metadata:
      name: minimal-pod
    spec:
      containers:
      - name: pod
        image: alpine:3.9
        command: ["tail", "-f", "/dev/null"]
    EOF

  args:
    chdir: "/home/{{ cluster_admin }}"    
  become: yes
  become_user: root
